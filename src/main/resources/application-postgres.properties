database=postgres
spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://localhost/music}
spring.datasource.username=${POSTGRES_USER:postgres}
spring.datasource.password=${POSTGRES_PASS:jk_16tRs*95bT}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always
